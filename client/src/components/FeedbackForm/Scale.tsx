import React from "react";

//Styling
import styles from "./FeedbackForm.module.css";

//this component will be mapped to each isFreeForm == false item of template
const Scale = () => {
  return (
    <div>
      <input type="radio"></input>
    </div>
  );
};

export default Scale;
